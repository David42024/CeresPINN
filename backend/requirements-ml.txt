# CeresPINN - Training environment (PyTorch PINN)
# ---------------------------------------------------------------------------
# Separated from requirements.txt (the inference runtime) so the lightweight
# FastAPI backend does not carry the heavy ML stack.
#
# Install (CPU default):
#     pip install -r backend/requirements-ml.txt
#
# For GPU support install the matching torch build FIRST, e.g. on Windows CUDA:
#     pip install torch --index-url https://download.pytorch.org/whl/cu121
#     pip install -r backend/requirements-ml.txt
# ---------------------------------------------------------------------------

torch>=2.2.0
numpy>=1.26.0
scipy>=1.11.0
scikit-learn>=1.4.0
pandas>=2.2.0
xarray>=2024.1.0
netCDF4>=1.6.0
tqdm>=4.66.0
python-dotenv>=1.0.0

# Optional reproducibility / experiment tracking
# tensorboard>=2.16
# mlflow>=2.10
